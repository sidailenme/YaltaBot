spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yalta
    username: root
    password: rootroot

  jpa:
    hibernate:
      ddl-auto: create-drop